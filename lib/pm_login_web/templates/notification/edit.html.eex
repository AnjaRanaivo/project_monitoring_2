<h1>Edit Notification</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.notification_path(@conn, :update, @notification)) %>

<span><%= link "Back", to: Routes.notification_path(@conn, :index) %></span>
